services:
  app:
    image: lsby-punch-in-dev
    build:
      context: ../../
      dockerfile: ./deploy/development/dockerfile
    ports:
      - 3000:3000
    env_file:
      - ../../.env/.env.development
    restart: always
    volumes:
      - ../../db:/opt/app/db
